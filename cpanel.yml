deployment:
  tasks:
    - export GIT_REPO="https://github.com/mmjxfv/icepack_baxtiary.git"
    - export BRANCH="main"
    - cd /home/bestvxoh/public_html/icepack
    - if [ ! -d .git ]; then git clone --branch $BRANCH $GIT_REPO . ; else git pull origin $BRANCH ; fi
    - echo "Deployment complete!"
